//per prima cosa otteniamo una parola dall'utente
let word = prompt("inserisci la parola che vuoi verificare :");
console.log(word);

//stabiliamo un ciclo for che ci stampi il nostro array di lettere
for (let i = 0; i < word.length; i++) {
    let lettere = word[i];
    console.log(lettere)
    
}
let lettere = word.split("");
console.log(lettere);
//for( let i = word.length; i >= 0; i--){         (questo era un ciclo for che riscriveva le parole al contrario)
    //let verifica = word[i];
   // console.log(verifica)}

let max = word.length
let min = i = 0

let verifica = word.split("max, min");
console.log(verifica);






